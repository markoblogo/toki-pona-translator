# v0.3.0 Integration Track (PoC)

Набор практических PoC-артефактов для трека `v0.3.x`.

## A) Готовые PoC-пакеты

- [adapters/browser-extension](adapters/browser-extension/README.md)
- [adapters/obsidian](adapters/obsidian/README.md)
- [adapters/logseq](adapters/logseq/README.md)
- [adapters/wordpress](adapters/wordpress/README.md)
- [adapters/ghost](adapters/ghost/README.md)

Каждый пакет включает `entry.json` с релизным манифестом (тип артефакта, entrypoints, versioned build notes).

## B) Build-плагины с route guard (allowlist/denylist)

`vite`/`webpack` helper-плагины поддерживают маршруты:

```ts
sitelenLayerVitePlugin({
  autoInit: true,
  routeInclude: ['/tp', '/toki-pona'],
  routeExclude: ['/admin', '/api/'],
  routeMatchMode: 'contains'
});
```

```js
new SitelenLayerWebpackPlugin({
  autoInit: true,
  routeInclude: ['/tp', '/toki-pona'],
  routeExclude: ['/admin', '/login'],
  routeMatchMode: 'contains'
});
```

Режимы сопоставления: `contains | startsWith | exact | regex`.

## C) Demo страницы

- `demo/integration/vite-webpack-route-guard.html`
- `demo/integration/extension-csp-flow.html`

В них отражены smoke-сценарии для CI/снапшотной проверки: route allow/deny и CSP-safe extension bootstrap.

## D) Что считать завершённым в минимальном PoC

- [x] Browser extension: route-guard в `content.js` + guard на двойной init.
- [x] Obsidian/Logseq: корректный `init`/`destroy`/re-init lifecycle.
- [x] WP/Ghost: безопасный шаблон инъекции с route-фильтрами.
- [x] Пакеты адаптеров готовы к публикации через единую структуру `entry.json`.

## F) Публикация v0.3.0-MVP (preflight pipeline)

- CI выполняет `npm run build`, `npm run pack:adapters`, `npm run smoke:adapters`, `npm run smoke:publication`.
- Для каждого адаптера создаётся zip-артефакт в `dist/adapters/<pkg>-<adapter>-v<version>.zip`.
- В workflow архивируются готовые адаптер-пакеты и npm-транспортный `.tgz` для ручной проверки.

## G) Release pipeline

- `.github/workflows/release-publish.yml` публикует релиз по событию:
  - push `v*` тегов
  - ручной `workflow_dispatch` (ввод `tag`)
- Шаги: `build` → `test` → `pack:adapters` → `smoke:adapters` → `smoke:publication` → `npm publish`.
- Финальный релиз автоматически создаёт GitHub release с приложениями: `dist/adapters/*.zip` и tarball `*.tgz`.

Для проверки staging-контура используйте `workflow_dispatch` с `dryRun=true` из
`release-publish.yml`: это прогоняет весь конвейер, включает валидацию тега и выгрузку артефактов в GitHub Release без публикации в npm.

## E) Migration/переход

См. [docs/MIGRATION_v0.3.0.md](docs/MIGRATION_v0.3.0.md)
